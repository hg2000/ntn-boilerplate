site_url: https://ntn-boilerplate.netlify.app/
logo_url: https://ntn-boilerplate.netlify.app/icon.png

publish_mode: editorial_workflow
backend:
  name: github
  branch: master
  repo: hg2000/ntn-boilerplate
  squash_merges: true

# used together with `npx netlify-cms-proxy-server`. https://www.netlifycms.org/docs/beta-features/#working-with-a-local-git-repository
local_backend: true

media_folder: static/img
public_folder: /img

# Docs: https://www.netlifycms.org/docs/configuration-options/#collections
collections:
  - label: 'Site settings'
    name: 'site'
    files:
      - label: 'Info'
        name: 'info'
        file: 'content/site/info.json'
        format: 'json'
        editor:
          preview: false
        fields:
          - { label: Site Name, name: sitename, widget: string, hint: 'This is used for the site title' }
          - {
              label: Site Description,
              name: sitedescription,
              widget: string,
              hint: 'This is used as descriptive text for links that link to this site',
            }
          - {
              label: Site Language,
              name: sitelang,
              widget: string,
              pattern: ['^[a-z]{2}([-])?([A-Za-z]{2})?$', 'Must match "xx" or "xx-XX" syntax (ex. "en" or "sv-SE")'],
            }
          # - { label: Site Email, name: siteemail, widget: string, required: false }


  - name: 'changelogs'
    label: 'Change logs'
    label_singular: 'Change log'
    folder: 'content/changelogs'
    create: true
    format: 'frontmatter'
    slug: '{{slug}}'
    preview_path: 'changelogs/{{slug}}'
    fields:
      - {
          label: 'Type',
          name: 'type',
          widget: 'select',
          default: 'feature',
          options: [{ label: 'Bug', value: 'bug' }, { label: 'Feature', value: 'feature' }],
        }
      - {
        label: 'Component',
        name: 'component',
        widget: 'select',
        default: 'feature',
        options: [ { label: 'User Interface', value: 'userinterface' }, { label: 'Interfaces', value: 'interfaces' } ],
      }
      - { label: 'Title', name: 'title', widget: 'string', required: true }
      - { label: 'Description', name: 'description', widget: 'string', required: false }


